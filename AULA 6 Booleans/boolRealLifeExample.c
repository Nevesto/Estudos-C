#include <stdio.h>

int main() {
    int myAge = 10;
    int votingAge = 18;

    if (myAge >= votingAge) {
        printf("Old enough to vote.");
    } else {
        printf("Not old enough to vote.");
    }
}