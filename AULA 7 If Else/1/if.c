#include <stdio.h>

int main() {
    // with integers

    if(10 > 4) {
        printf("\n10 is bigger than 4");
    }

    // with variables

    int x = 12;
    int y = 3;

    if(x > y) {
        printf("\nx is bigger than y");
    }
}